import React from "react";
import "./Quiz.css"; // Asegúrate de importar el archivo CSS correspondiente

const Quiz = () => {
  return (
    <div className="quiz-container">
      {/* Contenido de tu componente Quiz */}
    </div>
  );
};

export default Quiz;
